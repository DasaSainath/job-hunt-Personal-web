# Assets

Drop your files here:

- **`resume.pdf`** — your actual resume. The "Download resume" button on `index.html` points here.
- **`profile.jpg`** (optional) — your headshot, if you want to wire one into the hero.
- Any images referenced by `data/resume.json` or project cards.
